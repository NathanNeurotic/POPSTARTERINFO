# Site Seed Placeholder

Codex should replace this with a full Astro + Starlight documentation site.

Suggested commands:

```bash
npm create astro@latest .
npx astro add starlight
npx pagefind --site dist
```

Import data from `../data/` into `src/data/`.
